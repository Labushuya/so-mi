# Phase-2 placeholder. core-llm consumers don't need extra keep rules yet —
# the only public surface is interfaces + a Hilt-provided no-op. Real rules
# land in Phase 2.3 with the JNI-backed implementation.
