package io.somi.common

/**
 * Phase 1 placeholder so the module has a compilable source root.
 * Real utilities (Result wrappers, DispatcherProvider, Logger) land in Phase 2+.
 */
internal object CoreCommonMarker
